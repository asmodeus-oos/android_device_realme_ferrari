From 4e2860cd36272357f584079e282579950be5449a Mon Sep 17 00:00:00 2001
From: Bubun Das <bubundas17@gmail.com>
Date: Fri, 14 Aug 2026 16:32:03 +0000
Subject: [PATCH 1/2] evolution: ferrari-specific vendor config tweaks

Treat evolution_ lunch products like lineage_ in envsetup, allowlist
.repo for nsjail, and skip the generic Pixel fingerprint spoof on
ferrari.

Co-authored-by: Cursor <cursoragent@cursor.com>
---
 build/envsetup.sh            | 2 ++
 config/BoardConfigLineage.mk | 4 ++++
 config/evolution.mk          | 1 +
 3 files changed, 7 insertions(+)

diff --git a/build/envsetup.sh b/build/envsetup.sh
index 47bb6534..461f8faa 100644
--- a/build/envsetup.sh
+++ b/build/envsetup.sh
@@ -14,6 +14,8 @@ function check_product()
     fi
     if (echo -n $1 | grep -q -e "^lineage_") ; then
         LINEAGE_BUILD=$(echo -n $1 | sed -e 's/^lineage_//g')
+    elif (echo -n $1 | grep -q -e "^evolution_") ; then
+        LINEAGE_BUILD=$(echo -n $1 | sed -e 's/^evolution_//g')
     else
         LINEAGE_BUILD=
     fi
diff --git a/config/BoardConfigLineage.mk b/config/BoardConfigLineage.mk
index 172d2966..0bff8b06 100644
--- a/config/BoardConfigLineage.mk
+++ b/config/BoardConfigLineage.mk
@@ -4,6 +4,10 @@
 # Recovery
 BOARD_USES_FULL_RECOVERY_IMAGE ?= true
 
+# repo manifest (build-manifest.xml) updates .repo/*.json caches during the build.
+# nsjail requires an absolute allowlist path (relative .repo breaks WSL2 bind mounts).
+BUILD_BROKEN_SRC_DIR_RW_ALLOWLIST += $(abspath .)/.repo
+
 include vendor/lineage/config/BoardConfigKernel.mk
 
 ifeq ($(BOARD_USES_QCOM_HARDWARE),true)
diff --git a/config/evolution.mk b/config/evolution.mk
index 0112216d..127a06f0 100644
--- a/config/evolution.mk
+++ b/config/evolution.mk
@@ -8,6 +8,7 @@ ifeq ($(filter $(LINEAGE_BUILD), \
     tokay caiman komodo comet tegu \
     mustang blazer frankel rango \
     stallion \
+    ferrari \
     ),)
 PRODUCT_BUILD_PROP_OVERRIDES += \
     BuildFingerprint=google/mustang_beta/mustang:CANARY/ZP11.260717.006/16004061:user/release-keys
-- 
2.43.0

